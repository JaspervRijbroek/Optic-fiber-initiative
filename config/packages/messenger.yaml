framework:
    messenger:
        failure_transport: failed

        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    use_notify: false
            failed: 'doctrine://default?queue_name=failed&auto_setup=0'

        routing:
            App\Message\SendConfirmationEmail: async
